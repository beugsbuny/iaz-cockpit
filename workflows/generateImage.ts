export function generateAndPublishImage(prompt: string) {
  console.log("🖼️ Génération d'image pour :", prompt);
  return {
    success: true,
    imageUrl: "https://example.com/image.png"
  };
}